### 通用配置文件 ###
# 此部分包含编译配置、优化选项和基础软件包

CONFIG_DEVEL=y                               # 启用开发者模式，显示更多编译选项
CONFIG_TARGET_OPTIONS=y                      # 启用目标平台选项配置
CONFIG_TARGET_OPTIMIZATION="-O3 -pipe -march=armv8-a+crypto+crc -mcpu=cortex-a53+crypto+crc -mtune=cortex-a53"
                                              # ARM架构优化参数：
                                              # -O3: 最高级别优化
                                              # -pipe: 使用管道而非临时文件加速编译
                                              # -march: ARMv8-A架构，支持加密和CRC指令
                                              # -mcpu: Cortex-A53 CPU，支持加密和CRC
                                              # -mtune: 针对Cortex-A53优化

CONFIG_CCACHE=y                              # 启用编译缓存，加速后续编译
CONFIG_PACKAGE_default-settings=y            # 包含默认设置
CONFIG_PACKAGE_default-settings-chn=y        # 包含中国区默认设置（时区、源等）
CONFIG_TARGET_PER_DEVICE_ROOTFS=y            # 为每个设备生成独立的rootfs
CONFIG_LUCI_LANG_zh_Hant=n                   # 包含繁体中文语言支持

# 禁用的配置项（以# CONFIG_开头，is not set结尾）
# CONFIG_FEED_nss_packages is not set        # 禁用NSS软件包源
# CONFIG_FEED_sqm_scripts_nss is not set     # 禁用SQM NSS脚本源
# CONFIG_TARGET_ROOTFS_INITRAMFS is not set  # 不生成initramfs镜像（减少体积）

### USB驱动支持 ###
CONFIG_PACKAGE_kmod-usb2=n                   # USB 2.0 控制器驱动
CONFIG_PACKAGE_kmod-usb3=n                   # USB 3.0 控制器驱动
CONFIG_PACKAGE_kmod-usb-ohci=n               # OHCI USB控制器驱动（老设备）
CONFIG_PACKAGE_kmod-usb-uhci=n               # UHCI USB控制器驱动（Intel）
CONFIG_PACKAGE_kmod-usb-ehci=n               # EHCI USB控制器驱动（USB 2.0高速）
CONFIG_PACKAGE_kmod-usb-xhci=n               # xHCI USB控制器驱动（USB 3.0+）

### ZRAM内存压缩交换 ###
CONFIG_PACKAGE_zram-swap=y                   # ZRAM内存压缩交换支持
CONFIG_PACKAGE_kmod-lib-lz4=y                # LZ4压缩算法内核模块
CONFIG_KERNEL_ZRAM_BACKEND_LZ4=y             # ZRAM使用LZ4压缩后端
CONFIG_KERNEL_ZRAM_BACKEND_LZO=y             # ZRAM使用LZO压缩后端（备用）
CONFIG_KERNEL_ZRAM_DEF_COMP_LZ4=y            # ZRAM默认使用LZ4压缩算法

### 系统工具软件包 ###
CONFIG_PACKAGE_tmux=y                        # 终端多路复用器（类似screen但更强大）
CONFIG_PACKAGE_dmesg=y                       # 内核消息查看工具
CONFIG_PACKAGE_htop=y                        # 交互式进程查看器（基础版）
CONFIG_PACKAGE_btop=n                        # 资源监控工具（现代版，图形化更好）
CONFIG_PACKAGE_fdisk=n                       # 磁盘分区工具（MBR格式）
CONFIG_PACKAGE_cfdisk=n                      # 交互式磁盘分区工具（curses界面）
CONFIG_PACKAGE_sgdisk=n                      # GPT分区工具（用于GPT格式磁盘）
CONFIG_PACKAGE_tune2fs=n                     # ext2/ext3/ext4文件系统调整工具
CONFIG_PACKAGE_ariang=n                      # Aria2 Web管理界面
CONFIG_PACKAGE_fstrim=n                      # SSD TRIM支持工具
CONFIG_PACKAGE_nano-full=y                   # Nano文本编辑器（完整功能版）
CONFIG_PACKAGE_autocore=y                    # 自动检测架构和温度
CONFIG_PACKAGE_coremark=y                    # CPU性能基准测试工具
CONFIG_PACKAGE_hdparm=n                      # 硬盘参数获取/设置工具
CONFIG_PACKAGE_smartmontools-drivedb=n       # SMART硬盘监控工具数据库
CONFIG_PACKAGE_zoneinfo-asia=y               # 亚洲时区数据
CONFIG_PACKAGE_acme-acmesh-dnsapi=n          # ACME DNS API支持（自动SSL证书）
CONFIG_PACKAGE_openssh-sftp-server=n         # OpenSSH SFTP服务器
CONFIG_PACKAGE_ddns-scripts-cloudflare=n     # CloudFlare DDNS脚本
CONFIG_PACKAGE_sqm-scripts-nss=y             # SQM QoS脚本（NSS加速版）
CONFIG_PACKAGE_luci-app-unblockneteasemusic=n# 网易云音乐解锁
CONFIG_PACKAGE_luci-app-ddns-go=n            # 动态域名解析
CONFIG_PACKAGE_luci-app-zerotier=n           # 内网穿透
CONFIG_PACKAGE_luci-app-openclash=n          # 代理软件
CONFIG_PACKAGE_luci-app-homeproxy=n
CONFIG_PACKAGE_luci-app-nikki=y
CONFIG_PACKAGE_luci-app-momo=n

### Luci主题和应用 ###
CONFIG_PACKAGE_luci-theme-aurora=y           # Aurora主题（极光主题）
CONFIG_PACKAGE_luci-app-aurora-config=n      # Aurora主题配置界面
CONFIG_PACKAGE_luci-theme-argon=y            # Argon主题（现代主题）
CONFIG_PACKAGE_luci-app-argon-config=n       # Argon主题配置界面
CONFIG_PACKAGE_luci-app-cpufreq=y            # CPU频率调整工具
CONFIG_PACKAGE_luci-app-ddns=n               # 动态DNS客户端
CONFIG_PACKAGE_luci-app-ttyd=y               # 网页终端（通过浏览器访问shell）
CONFIG_PACKAGE_luci-app-upnp=y               # UPnP自动端口转发
CONFIG_PACKAGE_luci-app-wolplus=n            # Wake-on-LAN网络唤醒增强版
CONFIG_PACKAGE_luci-app-samba4=n             # Samba文件共享服务（SMB/CIFS）
CONFIG_PACKAGE_luci-app-vlmcsd=n             # KMS激活服务器
CONFIG_PACKAGE_luci-app-aria2=n              # Aria2下载管理器界面
CONFIG_PACKAGE_luci-app-autoreboot=y         # 定时自动重启
CONFIG_PACKAGE_luci-app-wifischedule=n       # WiFi定时开关
CONFIG_PACKAGE_luci-app-uhttpd=n             # uHTTPd Web服务器管理
CONFIG_PACKAGE_luci-app-frps=n               # FRP服务器管理（内网穿透服务端）
CONFIG_PACKAGE_luci-app-frpc=n               # FRP客户端管理（内网穿透客户端）
CONFIG_PACKAGE_luci-app-diskman=n            # 磁盘管理工具
CONFIG_PACKAGE_luci-app-hd-idle=n            # 硬盘休眠工具
CONFIG_PACKAGE_luci-app-banip=n              # IP黑名单/防火墙管理
CONFIG_PACKAGE_luci-app-acme=n               # ACME自动SSL证书管理
CONFIG_PACKAGE_luci-app-arpbind=y            # ARP绑定（IP-MAC地址绑定）
CONFIG_PACKAGE_luci-app-usb-printer=n        # USB打印机共享
CONFIG_PACKAGE_luci-app-oaf=n                # OpenAppFilter应用过滤
CONFIG_PACKAGE_luci-app-sqm=n                # SQM QoS流量管理
CONFIG_PACKAGE_luci-proto-wireguard=n        # WireGuard VPN协议支持
CONFIG_PACKAGE_luci-app-wechatpush=n         # 微信推送通知
CONFIG_PACKAGE_luci-app-watchcat=n           # 看门狗监控（系统健康检查）
CONFIG_PACKAGE_luci-app-3cat=n               # 3CAT工具集（具体功能需查证）
CONFIG_PACKAGE_luci-app-smartdns=n           # SmartDNS智能DNS解析

### PassWall配置项 ###
# PassWall是OpenWrt上功能强大的透明代理工具
CONFIG_PACKAGE_luci-app-passwall=n           # PassWall主程序

# 透明代理实现方式选择
# CONFIG_PACKAGE_luci-app-passwall_Iptables_Transparent_Proxy is not set  # 禁用iptables实现
CONFIG_PACKAGE_luci-app-passwall_Nftables_Transparent_Proxy=n           # 启用nftables实现（现代）

# 功能模块选择（启用/禁用）
CONFIG_PACKAGE_luci-app-passwall_INCLUDE_Geoview=n       # 地理信息查看
CONFIG_PACKAGE_luci-app-passwall_INCLUDE_Haproxy=n       # HAProxy负载均衡

# 以下协议支持被禁用（注释掉表示不包含）
# CONFIG_PACKAGE_luci-app-passwall_INCLUDE_Hysteria is not set           # Hysteria协议
# CONFIG_PACKAGE_luci-app-passwall_INCLUDE_NaiveProxy is not set         # NaiveProxy
# CONFIG_PACKAGE_luci-app-passwall_INCLUDE_Shadowsocks_Libev_Client is not set  # SS Libev客户端
# CONFIG_PACKAGE_luci-app-passwall_INCLUDE_Shadowsocks_Libev_Server is not set  # SS Libev服务端
# CONFIG_PACKAGE_luci-app-passwall_INCLUDE_Shadowsocks_Rust_Client is not set   # SS Rust客户端
# CONFIG_PACKAGE_luci-app-passwall_INCLUDE_Shadowsocks_Rust_Server is not set   # SS Rust服务端
# CONFIG_PACKAGE_luci-app-passwall_INCLUDE_ShadowsocksR_Libev_Client is not set # SSR Libev客户端
# CONFIG_PACKAGE_luci-app-passwall_INCLUDE_ShadowsocksR_Libev_Server is not set # SSR Libev服务端
# CONFIG_PACKAGE_luci-app-passwall_INCLUDE_Shadow_TLS is not set         # Shadow-TLS
# CONFIG_PACKAGE_luci-app-passwall_INCLUDE_Simple_Obfs is not set        # 简单混淆
# CONFIG_PACKAGE_luci-app-passwall_INCLUDE_SingBox is not set            # SingBox
# CONFIG_PACKAGE_luci-app-passwall_INCLUDE_Trojan_Plus is not set        # Trojan+
# CONFIG_PACKAGE_luci-app-passwall_INCLUDE_tuic_client is not set        # TUIC客户端
# CONFIG_PACKAGE_luci-app-passwall_INCLUDE_V2ray_Geodata is not set      # V2Ray地理数据
# CONFIG_PACKAGE_luci-app-passwall_INCLUDE_V2ray_Plugin is not set       # V2Ray插件

# CONFIG_PACKAGE_luci-app-passwall_INCLUDE_Xray=n           # Xray核心（主要代理引擎）
# CONFIG_PACKAGE_luci-app-passwall_INCLUDE_Xray_Plugin is not set       # Xray插件
